# Copy to .env and fill in. NEVER commit real keys.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
BRAINTRUST_API_KEY=
WANDB_API_KEY=
# Model routing (WS-D / open decision #5)
# Force seeded demo personas even when PERSONA_MODEL + provider keys are present.
DEMO_DETERMINISTIC=1
PLANNER_MODEL=claude-sonnet-4-6
VERIFIER_MODEL=deterministic
PERSONA_MODEL=gpt-4o-mini
# Governed record (api/workproducts.py) — HMAC integrity-seal key.
# A non-secret demo fallback ("dev-demo-not-secret") is used if unset so tests/CI run offline;
# set a real value in production. This is tamper-evidence, not a legal signature.
WORKPRODUCT_SECRET=
