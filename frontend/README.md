# frontend/ — WS-H (Claude / Lovable)
Four prototype surfaces: (1) meeting side panel, (2) decision packet workspace,
(3) action diff drawer, (4) Agent Ops / Eval proof. Built in Lovable against the JSON
shapes in `core.schemas` (export via `make schemas-json`). Use mock data until the API lands.
