# Frontend data source.
# Default (unset or "true") = bundled mocks in src/data/* → pixel-identical to Lovable.
# Set false + a gateway URL to fetch live from the FastAPI gateway (`make serve` at repo root).
VITE_USE_MOCKS=true
VITE_API_URL=http://localhost:8000
