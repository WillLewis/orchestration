# evals/ — WS-G (Claude harness)
Braintrust/W&B harness, EvalPack runner, offline replay, the three-vertical eval runner,
failure taxonomy, scorecard. Consumes EvalTrace from all stages. DoD: `run(pack_id)` returns
EvalResults; three-vertical proof produces one scorecard across finance/legal/health.
